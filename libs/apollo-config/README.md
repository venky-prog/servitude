# apollo-config

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build apollo-config` to build the library.

## Running unit tests

Run `nx test apollo-config` to execute the unit tests via [Jest](https://jestjs.io).
