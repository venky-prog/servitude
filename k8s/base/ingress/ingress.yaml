apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: servitude
spec:
  ingressClassName: nginx

  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hive-router
                port:
                  number: 4000